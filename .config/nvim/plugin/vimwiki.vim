" TODO: convert to lua
let g:vimwiki_markdown_link_ext=1 " append extension to links for compatablity
let g:vimwiki_global_ext=0 " don't assume vimwiki ft if outside path
let g:vimwiki_list = [{'path': '~/.local/share/vimwiki', 'syntax': 'markdown', 'ext': '.md', 'nested_syntaxes': {'php': 'php'}}]
