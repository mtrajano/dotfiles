setopt share_history # share history between sessions
setopt inc_append_history # append to history immediately
setopt hist_ignore_space # do not add command to history if prepended with a space
setopt hist_ignore_dups # only add unique commands to history
setopt hist_find_no_dups # ctrl-r ignores dup results
